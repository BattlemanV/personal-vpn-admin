const CACHE='fn-v4';const PRECACHE=['/','/manifest.json'];const API_PATHS=['/dashboard','/peers','/peer','/backup','/parental','/tokens','/settings','/maintenance','/avatars','/diagnostics','/activity'];self.addEventListener('install',e=>{e.waitUntil(caches.open(CACHE).then(c=>c.addAll(PRECACHE)).then(()=>self.skipWaiting()))});self.addEventListener('activate',e=>{e.waitUntil(caches.keys().then(ks=>Promise.all(ks.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim()))});self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;const url=new URL(e.request.url);if(API_PATHS.some(p=>url.pathname===p||url.pathname.startsWith(p+'/'))){e.respondWith(fetch(e.request));return}e.respondWith((async()=>{try{const r=await fetch(e.request);if(r.ok){const c=await caches.open(CACHE);c.put(e.request,r.clone())}return r}catch{const c=await caches.match(e.request);return c||new Response('Offline',{status:503})}})())})